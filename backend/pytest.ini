[pytest]
testpaths = tests

# 기존 async 테스트가 @pytest.mark.asyncio 를 명시적으로 붙여 사용하므로 strict 모드를 쓴다.
# auto 로 두면 마크를 빠뜨린 async 테스트가 조용히 통과한 것처럼 보이지만,
# strict 에서는 마크 없는 async 테스트가 경고와 함께 드러난다.
asyncio_mode = strict
