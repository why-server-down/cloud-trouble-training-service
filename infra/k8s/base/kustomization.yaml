# secret.template.yaml 은 의도적으로 빠져 있다. 값이 없는 목록이고, 적용하면
# 실제 Secret 을 placeholder 로 덮어쓴다.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: afterfail
resources:
  - namespace.yaml
  - rbac.yaml
  - config.yaml
  - resourcequota.yaml
  - networkpolicy.yaml
  - backend.yaml
